<h1>詳細</h1>
<%= @post.body %>
<% @post.tags.each do |tag| %>
  <%= tag.name %>
<% end %>
  <%= link_to "編集", edit_post_path(@post) %>
<%= link_to "削除", @post, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
